const names = ['João', 'Irene', 'Fernando', 'Maria'];

const findNameWithFiveLetters = () => 
  // Adicione seu código aqui:
   names.find(nam => nam.length === 5);

console.log(findNameWithFiveLetters());