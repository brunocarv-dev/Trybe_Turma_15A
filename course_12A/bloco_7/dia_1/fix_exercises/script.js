/* function userInfo() {
  const userEmail = 'maria@email.com';

  // Toda expressão dentro do escopo da função userInfo tem acesso à variável userEmail
  
}
userInfo();
onsole.log(userEmail); */



/* let favoriteLanguage = "Javascript";
favoriteLanguage = "Python";
console.log(favoriteLanguage); // Erro

const favoriteTechnology = "Machine learning";
favoriteTechnology = "Facial recognition";
console.log(favoriteTechnology); // Facial recognition */


/* // Com o template literals
console.log(`Primeira linha;
Segunda linha;
Terceira linha;`
) // Perceba que a quebra de linha foi implementada na própria string sem a utilização de algum recurso de ling. de programação

// Sem o template literals:
console.log('Primeira linha;\n' + 'Segunda linha;\n' + 'Terceira linha;\n') */

// Situação em que é mais simples usar o operador ternário:
/* const checkIfElse = (age) => {
  if (age >= 18) {
    return `Você tem idade para dirigir!`;
  } else {
    return `Você ainda não tem idade para dirigir...`;
  }
};
 */
/* const checkTernary = age => (
  age >=18 ? `Você tem idade para dirigir!` : `Você ainda não tem idade para dirigir...`
);

console.log(checkTernary(19)); */